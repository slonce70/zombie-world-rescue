# Глибокий аудит «Операція: Порятунок Світу» (v26–v37)

Багатоагентний аудит (9 аудиторів по вимірах → адверсаріальна перевірка кожної high/critical
знахідки → синтез). 43 знахідки всього; 12 підтверджено як high/critical (0 хибнопозитивів
серед перевірених), 31 medium/low. Дата: 2026-06-20.

## 🔴 Виправити негайно (high)

### Реліз A — v38 «Кооп знову дружній»
- **F6** Шкода зомбі ×N гравців (`src/zombies.js:724,732,787` — `*coopMul()` на damage). Втрьох walker б'є 30, чардж боса 102. Прибрати coopMul з усіх 3 damage-виразів; лишити лише на HP (`:93`). Додати тест: кооп-урон == соло.
- **F18/F40** Кастом-герой невидимий у коопі (`coop.js:60` шле лише skin; `remoteplayer.js:38` `makeHero(skin)` без кольорів). Слати `hero` у roster; `makeHero(skin, info.hero)`. PROTO-бамп не треба.
- **F19** Реконект гостя = повний тіл-даун рівня посеред бою (`coop.js:258-262` хост безумовно шле `start`; гість без гарда `:220-223`). Хост читає `d.resume` → лише captureState; гість ігнорує повторний start у бою.

### Реліз B — v39 «Летить на телефоні» (перф)
- **F28** Дефолт `'auto'` = важкі тіні 2048² + ~340 дерев з тінями, без детекту слабкого пристрою (`main.js:497-502`, `world.js:1251`). Детект (hardwareConcurrency/deviceMemory/isTouch) → тач-дефолт shadow:1024, shadow-камера ~90.
- **F29** PointLight у кожному будинку при `'auto'` (`world.js:1941`, гейт `shadow>=2048`). Гейтити на явний `q==='high'`.

### Реліз C — v40 «Безпека й мова»
- **F37** Лайка в ніку проходить у кооп (клієнтський `cleanNick` без `nickIsBad`; `coop.js:27-31,246`). Імпортувати `worker/nick.mjs` у coop.js, фільтрувати нік.
- **F34** «Яку країну рятуємо?» не перекладено (`main.js:618`, ключа нема в en/ru) — регресія «спершу ГРАТИ».
- **F35** Секція гардероба «Танці (N)» не перекладено (зайвий провідний `</div>` у `en.js:64`/`ru.js:64`).
- **F36** Тост «Браузер не зберігає прогрес» лишається укр. (`main.js:429`).

## 🟠 medium (Реліз D — v41 «Тач-UX і баланс»)
- **F12** Глобус на тачі: drag-обертання не працює (mouse-only хендлери), підказка «🖱️ Обертай» вводить в оману. Тап-вибір працює (синтетичний click). Додати touch/pointer + i18n підказки.
- **F1** 🔭-кнопка оптики накладається на кнопку вогню/гаджета у звичайному тач-режимі (`styles.css:740`).
- **F2** Немає тач-кнопки командних пінгів (📣) у коопі.
- **F3** «Поверни телефон» не показується на планшетах у портреті (>620px).
- **F7** Щитоносець: 500hp щита непробивний пістолетом. **F8** Стрільці-зомбі на навчальній Україні. **F9** Магнум/снайпер майже без набоїв. **F10** Базука самопідрив зблизька.
- **F14** Глава 1: кроки рахуються в Шторм/Арені. **F15** Кооп без worker дає тех-текст дитині.

## 🟠 medium (Реліз E — v42 «Сейв і санітизація»)
- **F24+F27** `saveHasProgress()` ігнорує кастом-герой/ціль/монети/медалі → ризик тихого перезапису. Розширити whitelist.
- **F25** Імпорт файлу може бути скасований новішим хмарним ts. **F26** Шаллоу-merge сейва (NaN-ризик).
- **F20** nade/rocket гостя без дистанц-гейту. **F21** реанімація хоста без перевірки близькості. **F23** fountain без rate-limit.
- **F42** Ціль очищається після 1-ї покупки багаторівневого товару (`shop.js:234`). **F41** Боси у `bestiary['boss']` — сміття.

## 🟢 low (беклог)
F4 (audio gesture), F5 (hold-actions тач), F11 (dmg-крива), F13 (два входи в кампанію), F16 (згорнути список країн), F17 (автостарт оминає онбординг), F22 (N сабмітів ліги), F30/F31/F32/F33 (перф-доточування: rig-культинг, мертві зомбі, адаптивна роздільність лише вниз, незапечені ландмарки).

## ✅ Перевірено — НЕ проблема
- **F38** Режим Малюк — коректна м'яка допомога (конус ~24°, без автовогню, лише тач, локальний). Чесний.
- **F39** Мегабокс/магазин — без хижацької монетизації (лише ігрові монети, pity, без реальних платежів).
- **F43** BESTIARY будується через t() на імпорті, але setLang→reload → коректна мова.

## Порядок робіт
v38 (кооп) → v39 (перф) → v40 (безпека+i18n) → v41 (тач-UX+баланс) → v42 (сейв+санітизація) → беклог low.
Принцип: спершу те, що ламає ключові дитячі фічі й перф на головній (тач) платформі; не чіпати робоче.
