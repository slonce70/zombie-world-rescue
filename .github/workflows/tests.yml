name: tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '17 2 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  release-gate:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    name: release-gate (${{ matrix.shard }})
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Full release shard
        run: SLOW=4 SHARD_INDEX=${{ matrix.shard }} SHARD_TOTAL=4 node test/release.mjs

  coop-gate:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    name: coop-gate (${{ matrix.shard }})
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Full coop shard
        run: SLOW=4 RETRY=1 SHARD_INDEX=${{ matrix.shard }} SHARD_TOTAL=4 node test/coop-release.mjs

  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Version sync
        run: node test/version-sync.mjs
      - name: Relay reconnect edge
        run: SLOW=2 node test/relay-reconnect.mjs || (echo "retry relay-reconnect" && SLOW=2 node test/relay-reconnect.mjs)
      - name: Start static server
        run: python3 -m http.server 8741 &
      - name: Smoke
        run: node test/smoke.mjs
      - name: Campaign progression
        run: node test/campaign.mjs && node test/ukraine-rebuild-moon.mjs && node test/spain-manor.mjs
      - name: Version check
        run: node test/version-check.mjs
      - name: Cloud save and policy
        run: SLOW=2 node test/cloudsave.mjs || (echo "retry cloudsave" && SLOW=2 node test/cloudsave.mjs)
      - name: i18n
        run: node test/i18n.mjs
      - name: Coop damage
        run: node test/coop-damage.mjs
      - name: Coop roster trust boundary
        run: node test/coop-nick.mjs
      - name: Save migration
        run: node test/save-migration.mjs

  e2e:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Start static server
        run: python3 -m http.server 8741 &
      - name: UKR campaign
        timeout-minutes: 45
        run: SLOW=4 node test/e2e.mjs

  expedition:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Solo expedition
        run: npm run test:expedition
      - name: Coop expedition
        run: npm run test:coop-expedition

  front:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: World Front domain, browser, mobile, metrics and co-op
        run: npm run test:front

  deploy-pages:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [smoke, expedition, front]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
