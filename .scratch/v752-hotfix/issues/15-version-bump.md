# 15 — Реліз v752: бамп версії й changelog

**What to build:** фінальний тікет латки. За `CLAUDE.md` версію треба бампити **в трьох місцях
одразу**, інакше авто-оновлення й офлайн-кеш зламаються:

- `src/main.js` — `APP_VERSION`
- `version.json` — поле `v`
- `sw.js` — `CACHE`

`PROTO_VERSION` у `src/net/protocol.js` **лишається 26** — формат кооп-повідомлень у цій
латці не змінюється, тож старі вкладки далі заходять у кімнати.

Якщо в ході латки з'явились нові ESM-модулі — вони мусять потрапити в `SHELL` у `sw.js`,
інакше офлайн зламається.

Changelog пишеться мовою, зрозумілою дитині й батькові: що змінилось з погляду гравця,
а не з погляду коду.

**Blocked by:** 01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14 — їде останнім.

**Status:** ready-for-agent

- [ ] `APP_VERSION`, `version.json`, `sw.js` CACHE = 752
- [ ] `PROTO_VERSION` лишився 26
- [ ] Нові модулі (якщо є) додані в `SHELL`
- [ ] `node test/version-sync.mjs` зелений
- [ ] `node test/sw-cache.mjs` зелений
- [ ] `node test/i18n-parity.mjs` зелений
- [ ] `CHANGELOG.md` має розділ v752 з людськими формулюваннями
- [ ] У changelog згадано, що виправлення воркера потребують окремого `wrangler deploy`
