# 02 — Лічильник світу: клієнт і глобус

**What to build:** гра шле свій внесок у лічильник і показує спільне число там, де
дитина його побачить.

## Внесок

`LobbyClient` (`src/net/lobby.js`) уже вміє слати разовий вантаж поза розкладом —
дивись `announceDayScore(wave)`. Зроби так само.

**Метрика має бути ЧЕСНОЮ.** У сейві лічильника врятованих людей немає; у грі є місії
типу `rescue`/`shiprescue`, де дитина реально звільняє людей (`m.rescueProgress`,
`src/missionpool.js:1870`). Візьми те, що справді відбулось у забігу, і не вигадуй
число заради красивої цифри. Якщо чесної метрики немає — скажи це у звіті, а не
підганяй.

## Показ

Спільне число має жити на видному місці — глобус або панель мультиплеєра
(`_renderSide` у `src/ui/coopui.js:77` уже малює дані лобі).

**Порожній світ виглядає сумно.** Якщо гравців мало, добове число буде маленьким і
радше демотивуватиме. Тому: нижче порога — показуй ціль ТИЖНЯ замість доби. Поріг
обери сам і поясни в коментарі.

Нові рядки `t('…')` — в `en.js` і `ru.js` (гейт `node test/i18n-parity.mjs`).

**Blocked by:** 01

**Status:** done

- [x] Внесок їде у воркер після забігу, не частіше за потрібне
- [x] Метрика відповідає тому, що дитина справді зробила
- [x] Число видно, і воно зрозуміле дитині 7 років
- [x] Мало гравців — показується тижневе, а не сумне добове
- [x] Немає інтернету — блок просто не показується, гра не ламається
- [x] `node test/i18n-parity.mjs` зелений

## Як зроблено

- Метрика — `level.missions.civilians.length`: люди, які фізично вийшли з хліва /
  маєтку / підземелля замку / корабля TUR і пішли за гравцем. Іншого чесного
  лічильника врятованих у сейві немає, вигаданих формул не вводили.
- Шлеться в `endLevel()` (єдина точка виходу з рівня — перемога, смерть, вихід,
  етап експедиції), одним пінгом `{saved}` через `LobbyClient.announceSaved`.
  У коопі рахує лише авторитет: гість бачить копії тих самих людей.
- Показ — `#world-saved` на глобусі під ціллю тижня, одне читання `/lobby/state`
  на хвилину. Поріг переходу на тижневе число — `WORLD_DAY_MIN = 100`
  (`src/net/lobby.js`, там і пояснення).
- Тест: `test/world-saved-unit.mjs` (у джобі `quick`).
