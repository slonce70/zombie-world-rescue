# 06 — Реліз v780

- `APP_VERSION` (`src/main.js`), `version.json`, `CACHE` у `sw.js` → 780
  (гейт `node test/version-sync.mjs`);
- `PROTO_VERSION` бампати ЛИШЕ якщо змінився формат кооп-повідомлень;
- нові ESM-модулі — у `SHELL` у `sw.js` (гейт `node test/sw-cache.mjs`);
- `CHANGELOG.md` — секція v780 дитячою українською, як v752/v760/v770;
- **воркер деплоїться ОКРЕМО**: `cd worker && npx wrangler deploy`. Тікет 01 змінює
  воркер, тож без цього кроку лічильник світу в проді не зʼявиться.

**Blocked by:** усі попередні

**Status:** done

- [x] Три місця версії збігаються
- [x] Секундні гейти зелені
- [x] CHANGELOG написаний для дитини
- [x] У звіті сказано, що воркер потребує окремого деплою
