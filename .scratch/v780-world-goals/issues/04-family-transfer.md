# 04 — Сімейна передача прогресу

**What to build:** код відновлення (8 знаків) уже існує — `src/net/cloudsave.js:230`,
`SaveVault` у воркері. Зараз це службова кнопка в «Мій прогрес». Треба подати його
як ФІЧУ: «грай на планшеті й на компі».

Нового коду майже не потрібно — потрібен нормальний екран і пояснення дитячою мовою:
що це за код, навіщо його записати, як ним скористатись на іншому пристрої, і що
станеться з тим прогресом, який уже є на другому пристрої (це найважче місце —
дитина може затерти собі гру, і вона мусить розуміти це ДО того, як натисне).

Попередження про перезапис — не дрібним шрифтом, а зрозумілим питанням.

**Blocked by:** —

**Status:** done

- [x] Код видно, його легко записати або скопіювати
- [x] Пояснення зрозуміле дитині, а не розробнику
- [x] Перед перезаписом чужого прогресу — явне попередження, що саме зникне
- [x] Скасувати можна на будь-якому кроці
- [x] Перевірено в браузері на мобільному розмірі

**Як зроблено:** панель `#overlay-progress` стала «💾 МОЯ ГРА ВСЮДИ»: два пронумеровані
кроки (взяти код тут / ввести код там), код 30px з `user-select: all` і кнопкою
«📋 Скопіювати», файл-копія — тихим блоком для дорослих.

Системний `confirm()` прибрано: обидва шляхи заміни (код і файл) ідуть через крок-екран
`#progress-warn` — червона рамка, «✋ ЗАЧЕКАЙ! ТУТ УЖЕ Є ТВОЯ ГРА», список конкретики з
`progressLoss()` (країни / монети / зірки / улюбленці на імена) і питання. Безпечна
відповідь — велика жовта «↩️ Ні, залишити цю гру», заміна — червона нижче.

Тест: `test/transfer-warn-unit.mjs` (у джобі `quick`) — 7 перевірок, зокрема «adopt не
викликано без явного так» і «панель не повертається до системного confirm()».
