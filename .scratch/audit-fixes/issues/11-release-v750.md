# 11 — Реліз v750: версія, документація, фінальна перевірка

**What to build:** Зібрати все зроблене в один випуск. Гравець після оновлення бачить нову версію,
не втрачає прогрес і працює офлайн. Документація описує те, що є насправді.

Окремо: документація зараз розходиться з кодом у дрібницях, які збивають з пантелику —
README називає попередню версію, посилається на теку спек, у якій лежить один файл; TODO
розповідає про блокери версії, випущеної багато релізів тому.

**Blocked by:** 01, 02, 03, 04, 05, 06, 07, 08, 09, 10 — це фінальний тікет партії.

**Status:** ready-for-agent

- [ ] Номер версії піднято до 750 в усіх трьох місцях одночасно; `node test/version-sync.mjs` зелений
- [ ] Усі нові модулі є в списку офлайн-кешу; `node test/sw-cache.mjs` зелений
- [ ] `node test/i18n-parity.mjs` зелений — жодного напису без перекладу
- [ ] `node test/save-migration.mjs` зелений — старий сейв відкривається без втрат
- [ ] `node test/smoke.mjs` зелений
- [ ] Швидкі доменні юніти зелені
- [ ] CHANGELOG має запис v750, написаний для гравця, а не для розробника
- [ ] README називає актуальну версію й не посилається на те, чого немає
- [ ] TODO описує поточний стан, а не блокери старої версії
- [ ] Гру перевірено в реальному браузері: глобус, старт країни, бій, драфт — без помилок консолі
